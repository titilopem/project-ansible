- name: Install Java
  hosts: all
  become: true
  tasks:
    - name: Update package lists (for Ubuntu)
      apt:
        update_cache: yes
      when: ansible_os_family == 'Debian'

    - name: Install Python 3 (for CentOS)
      raw: sudo yum install -y python3
      when: ansible_os_family == 'RedHat'

    - name: Update package lists (for CentOS 7)
      yum:
        name: '*'
        state: latest
      when: ansible_os_family == 'RedHat' and ansible_distribution_major_version == '7'

    - name: Update package lists (for CentOS 8)
      dnf:
        name: '*'
        state: latest
      when: ansible_os_family == 'RedHat' and ansible_distribution_major_version == '8'

    - name: Install OpenJDK 11 (for Ubuntu)
      package:
        name: openjdk-11-jdk
        state: present
      when: ansible_os_family == 'Debian'

    - name: Install OpenJDK 11 (for CentOS 7)
      package:
        name: java-11-openjdk
        state: present
      when: ansible_os_family == 'RedHat' and ansible_distribution_major_version == '7'

    - name: Install OpenJDK 11 (for CentOS 8)
      package:
        name: java-11-openjdk
        state: present
      when: ansible_os_family == 'RedHat' and ansible_distribution_major_version == '8'

    - name: Install Java 8 on CentOS 7
      yum:
        name: java-1.8.0-openjdk
        state: present
      when: ansible_os_family == 'RedHat' and ansible_distribution_major_version == '7'

    - name: Install Java 8 on CentOS 8
      dnf:
        name: java-1.8.0-openjdk
        state: present
      when: ansible_os_family == 'RedHat' and ansible_distribution_major_version == '8'

    - name: Install Java on Amazon Linux
      command: sudo dnf install -y java-1.8.0-openjdk
      when: ansible_os_family == 'Amazon'

    - name: Print Java version
      command: java -version
      register: java_version
      ignore_errors: yes  # Ignore errors if the 'java' command is not found

    - debug:
        var: java_version.stdout_lines

    - name: Debug - Installation failed
      debug:
        msg: "Java installation failed"
      when: java_version.failed
...