- name: Get Maven home directory
  find:
    paths: /usr /opt  # Add additional paths as needed
    patterns: mvn
    recurse: yes
  register: maven_home

- name: Display Maven home directory
  debug:
    var: maven_home.files | map(attribute='path') | first
