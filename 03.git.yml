- name: Manage Apache Tomcat
  hosts: group1
  become: true

  tasks:
    - name: Set JAVA_HOME
      ansible.builtin.set_fact:
        java_home_path: "/usr/lib/jvm/java-11-openjdk-amd64"

    - name: Set environment variables
      ansible.builtin.lineinfile:
        path: "/etc/environment"
        line: "JAVA_HOME={{ java_home_path }}"
        create: yes

    - name: Set Tomcat service name based on distribution
      ansible.builtin.set_fact:
        tomcat_service_name: >-
          {%- if ansible_distribution in ['Ubuntu', 'Debian'] -%}
          tomcat8
          {%- else -%}
          tomcat
          {%- endif -%}

    - name: Reload systemd
      ansible.builtin.command: "systemctl daemon-reload"

    - name: Check if shutdown.sh exists
      ansible.builtin.stat:
        path: "/opt/tomcat/bin/shutdown.sh"
      register: shutdown_script

    - name: Shutdown Tomcat service
      ansible.builtin.command: "/bin/bash -c 'source /etc/environment && /opt/tomcat/bin/shutdown.sh'"
      ignore_errors: yes  # Ignoring errors if script is not found or service not running
      when: shutdown_script.stat.exists

    - name: Check if startup.sh exists
      ansible.builtin.stat:
        path: "/opt/tomcat/bin/startup.sh"
      register: startup_script

    - name: Start Tomcat service
      ansible.builtin.command: "/bin/bash -c 'source /etc/environment && /opt/tomcat/bin/startup.sh'"
      ignore_errors: yes  # Ignoring errors if script is not found or service not running
      when: startup_script.stat.exists
...