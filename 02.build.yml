---
- name: Build WebApp with Maven
  hosts: n4c

  tasks:
    - name: Create Workspace on n4c
      become: true
      shell: "mkdir -p ~/ansibleproject"

    - name: Build the webapp using Maven
      command: "mvn clean package"
      args:
        chdir: "~/ansibleproject"
...
